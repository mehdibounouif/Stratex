# Stratex runtime dependencies
# pip-tools belongs in requirements-dev.txt, not here — removed.
# PyYAML pinned to avoid pip resolver spinning through versions mid-Docker build.

# Core data
pandas>=2.1.3
numpy>=1.26.2
scipy>=1.11.4

# Finance data
yfinance>=0.2.50
alpaca-trade-api>=3.2.0

# API & async
fastapi>=0.110.0
uvicorn>=0.27.0
aiohttp>=3.11.0
requests>=2.31.0
websockets<11

# Dashboard
streamlit>=1.29.0
plotly>=6.6.0
streamlit-autorefresh>=1.0.1

# Scheduler & config
apscheduler>=3.10.4
python-dotenv>=1.0.0
pytz>=2023.3.post1
python-dateutil>=2.8.2

# ML
scikit-learn>=1.3.2

# Pinned transitive deps that caused Docker timeouts
PyYAML==6.0.1